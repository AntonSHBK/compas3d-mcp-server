"""MCP server assembly point."""

import logging


logger = logging.getLogger(__name__)


def run_server() -> None:
    """Run the MCP transport when it is connected in a later iteration."""
    logger.info("The MCP transport has not been implemented yet.")
